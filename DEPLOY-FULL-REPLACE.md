# نصب کامل مجدد کد ناوراکار (بدون لمس دیتابیس)

این بسته یک **جایگزینی کامل کد** است — همهٔ فایل‌های `navracar-app/` و
`public_html/` یکجا با نسخهٔ تازه عوض می‌شوند، بدون نیاز به این‌که خودتان
فایل‌ها را تک‌تک با نسخهٔ قبلی مقایسه یا merge کنید. **دیتابیس و فایل `.env`
دست‌نخورده می‌مانند** — هیچ داده‌ای (آگهی‌ها، سرنخ‌ها، تنظیمات، حساب‌های
مدیر) از بین نمی‌رود.

## چرا این‌بار «vendor» داخل بسته نیست؟

پوشهٔ `vendor/` (کتابخانه‌های PHP) خیلی حجیم است (حدود ۴۵۰ مگابایت) و از
وقتی سایت شما راه‌اندازی شد، **هیچ کتابخانهٔ جدیدی به آن اضافه نشده** — پس
همان `vendor/` فعلی‌تان کاملاً درست و به‌روز است و لازم نیست دوباره
جایگزین شود. این بسته فقط شامل کد واقعی برنامه (که در طول به‌روزرسانی‌های
قبلی عوض شده) است، به همراه فایل‌های عمومی سایت (`public_html`) — و در
مجموع کمتر از ۳ مگابایت است.

## فایل‌های این بسته
- `navracar-app.zip` — **کل** محتوای پوشهٔ `navracar-app/` (به‌جز `vendor/`
  و `.env`).
- `public_html-contents.zip` — **کل** محتوای پوشهٔ `public_html/` (این یک
  بستهٔ کاملاً مستقل و آمادهٔ اکسترکت مستقیم است؛ نیازی نیست هیچ زیرپوشه‌ای
  را دستی جابه‌جا کنید).

## مراحل نصب

### مرحلهٔ ۱: پشتیبان‌گیری (احتیاطی)
از پوشه‌های `navracar-app/` و `public_html/` فعلی‌تان یک نسخهٔ فشرده در
File Manager بگیرید (کلیک راست ← Compress) تا اگر چیزی درست نشد بتوانید
برگردید.

### مرحلهٔ ۲: navracar-app/ — جایگزینی کامل (به‌جز vendor و .env)
۱. فایل `.env` را از داخل `navracar-app/` جایی امن کپی کنید (یا فقط
   مطمئن شوید که در طول مراحل بعدی حذفش نمی‌کنید).
۲. داخل `navracar-app/`، **همهٔ فایل‌ها و پوشه‌ها را انتخاب کنید به‌جز
   `.env` و `vendor/`** و حذفشان کنید. (اگر مطمئن نیستید کدام‌ها را نگه
   دارید، ساده‌تر است: همه را نگه دارید و در مرحلهٔ بعد فقط Overwrite
   بزنید — چون `navracar-app.zip` اصلاً فایلی به نام `vendor` ندارد، به آن
   پوشه دست نمی‌زند.)
۳. `navracar-app.zip` را داخل `navracar-app/` آپلود و Extract کنید،
   «Overwrite» را بزنید.
۴. مطمئن شوید `.env` و `vendor/` شما دست‌نخورده سر جایشان مانده‌اند.

### مرحلهٔ ۳: public_html/ — جایگزینی کامل
۱. داخل `public_html/`، **همهٔ فایل‌ها و پوشه‌ها را پاک کنید** — اینجا
   چیزی مثل `.env` یا دیتابیس نیست که نگرانش باشید؛ همه‌چیز این پوشه از
   کد ساخته می‌شود.
   (اگر فایل `setup-once.php` هنوز آنجا مانده، آن را هم پاک کنید — دیگر
   لازم نیست، چون دیتابیس و حساب مدیرتان از قبل وجود دارند.)
۲. `public_html-contents.zip` را همان‌جا آپلود و Extract کنید — نتیجه
   باید مستقیم این‌ها باشد: `public_html/index.php`, `public_html/build/`,
   `public_html/.htaccess`, `public_html/robots.txt`,
   `public_html/favicon.ico`.

⚠️ اگر بعد از Extract یک پوشهٔ اضافه به نام `public_html/public_html/` یا
`public_html/navracar/` دیدید، یعنی zip در یک زیرپوشه باز شده — محتوایش را
یک پله بالا بیاورید (Cut → Paste در `public_html/`) و پوشهٔ خالی‌شده را حذف
کنید.

### مرحلهٔ ۴: بررسی نهایی
۱. `https://navracar.com` را با Hard Refresh (Ctrl+Shift+R) باز کنید.
۲. صفحهٔ اصلی، `/calculator`، `/car-prices` و یک صفحهٔ آگهی خودرو را چک
   کنید — باید هرکدام کامل و با طراحی درست (رنگ‌ها، فونت) دیده شوند.
۳. حالت موبایل (F12 → آیکون گوشی، یا واقعاً با گوشی) را چک کنید — با زدن
   آیکون ☰ کنار لوگو باید منو باز شود.
۴. `/admin/login` را باز کنید و با همان حساب مدیر قبلی‌تان وارد شوید —
   چون دیتابیس دست‌نخورده مانده، همان حساب کار می‌کند.
۵. روی یک آگهی خودرو، «درخواست پیش‌فاکتور» را بزنید و مطمئن شوید PDF
   ساخته و دانلود می‌شود (این یعنی `vendor/` قدیمی‌تان هنوز درست کار
   می‌کند).

### اگر مرحلهٔ ۵ (دانلود PDF) کار نکرد
یعنی احتمالاً پوشهٔ `vendor/` شما یک کتابخانه (برای ساخت PDF) را ندارد. در
این صورت به من پیام دهید تا یک بستهٔ جداگانه فقط برای همان یک پوشه
(`vendor/barryvdh` و `vendor/dompdf`) برایتان بفرستم — نیازی به دانلود
دوبارهٔ کل `vendor/` نیست.

## این بسته قبل از تحویل واقعاً تست شد
یک محیط کامل شبیه‌سازی شد: کد فعلی گیت‌هاب با `composer install` و
`npm run build` واقعی ساخته شد، سپس همین دو بسته (`navracar-app.zip` +
`public_html-contents.zip`) کنار یک `vendor/` واقعی (که خودش هم از همین
کد ساخته شده بود، برای شبیه‌سازی vendor موجود شما) روی دیتابیس MySQL واقعی
قرار گرفت و با مرورگر واقعی (Chromium/Playwright) تأیید شد:
- صفحهٔ اصلی، محاسبه‌گر، فهرست قیمت خودروها، صفحهٔ یک آگهی، sitemap.xml،
  و ورود ادمین همگی ۲۰۰ برگرداندند.
- منوی موبایل با زدن آیکون ☰ باز شد و لینک‌ها دیده شدند.
- چارت زمان‌بندی پرداخت‌ها روی صفحهٔ آگهی درست رندر شد.
- **درخواست واقعی پیش‌فاکتور زده شد و فایل PDF واقعی با فونت فارسی خوانا
  ساخته و دانلود شد.**
