# به‌روزرسانی ناوراکار — نسخهٔ جدید (پیش‌فاکتور PDF + رفع چند ایراد)

این یک **بستهٔ به‌روزرسانی** است، نه نصب از صفر — فرض بر این است که سایت شما از
قبل روی `navracar.com` نصب و در حال کار است (طبق راهنمای `DEPLOY-ROOT-INSTALL.md`
قبلی) و یک پوشهٔ `navracar-app/` کنار `public_html/` دارید.

**نیازی به ایمپورت مجدد دیتابیس یا اجرای دوبارهٔ `setup-once.php` نیست** — این
نسخه هیچ جدول یا ستون جدیدی به دیتابیس اضافه نکرده؛ فقط کد برنامه عوض شده.

## چه چیزهایی در این نسخه درست/اضافه شد

1. **پیش‌فاکتور PDF خودکار + ایمیل**: وقتی مشتری از صفحهٔ یک آگهی خودرو دکمهٔ
   «درخواست پیش‌فاکتور» را بزند و فرم را پر کند، بلافاصله یک فایل PDF کامل و
   حرفه‌ای (با فونت فارسی، تفکیک همهٔ هزینه‌ها، جمع‌بندی، اطلاعات تماس) ساخته
   می‌شود — هم لینک دانلود آنی نشان داده می‌شود و هم (در صورت وارد کردن ایمیل)
   برای مشتری ایمیل می‌شود. همین قابلیت دانلود PDF به صفحهٔ پیش‌فاکتور رسمی در
   پنل مدیریت («پیش‌فاکتورها و فروش») هم اضافه شد.
2. **هزینهٔ صدور مجوز** حالا به‌صورت ردیف جداگانه در جدول صورت‌هزینهٔ
   محاسبه‌گر اصلی دیده می‌شود (قبلاً با هزینهٔ حمل دریایی ادغام و پنهان بود).
3. **اسکرول ویزارد محاسبه‌گر** اصلاح شد — دیگر با هر مرحله به بالای مطلق صفحه
   پرش نمی‌کند؛ به ابتدای کارت مرحله می‌رود و بخش‌های تبلیغاتی بالای صفحه در
   مراحل بعدی مخفی می‌شوند.
4. **هدر صفحهٔ محاسبه‌گر با بقیهٔ سایت یکی شد** — لینک اشتباه «سایت اصلی»
   (دامنهٔ غلط `navaracar.com`) اصلاح شد و آیتم‌های منوی قابل مدیریت از پنل هم
   در آن نمایش داده می‌شوند.
5. در صفحهٔ نمایش آگهی خودرو: مشخصات «نوع فروشنده» حذف شد، برچسب‌های برند/
   دسته/بازهٔ قیمت اضافه شدند، و مسیر بالای صفحه (breadcrumb) حالا برند خودرو
   را هم نشان می‌دهد.
6. برچسب‌های بازهٔ سی‌سی در چند فرم با اعداد یکنواخت سایت هماهنگ شدند.
7. دریافت خودکار آگهی از دابیزل: پیام خطای دقیق‌تری نشان می‌دهد (کدام فیلد
   استخراج نشد) وقتی دابیزل درخواست را مسدود کند — یادآوری: اگر دریافت خودکار
   کار نکرد، همیشه می‌توانید HTML صفحه را در همان فرم دستی پیست کنید (گزینهٔ
   «یا HTML صفحه را دستی پیست کنید» از قبل در فرم وجود دارد).

## فایل‌های این بسته
- `navracar-app-update.zip` — کد به‌روزشدهٔ لاراول + `vendor/` (شامل پکیج جدید
  `barryvdh/laravel-dompdf` برای ساخت PDF) + فونت فارسی موردنیاز.
- `public_html-update.zip` — فایل‌های به‌روزشدهٔ `public_html` (شامل CSS جدید).

## مراحل به‌روزرسانی

### مرحلهٔ ۱: پشتیبان‌گیری (احتیاطی)
از پوشهٔ `navracar-app/` فعلی‌تان یک نسخهٔ فشرده در File Manager بگیرید (کلیک
راست روی پوشه ← Compress) تا اگر چیزی درست نشد، بتوانید برگردید. دیتابیس را هم
از phpMyAdmin با Export یک نسخه پشتیبان بگیرید.

### مرحلهٔ ۲: جایگزینی کد برنامه
۱. فایل `.env` فعلی‌تان را از داخل `navracar-app/` جایی امن کپی کنید — **این
   فایل حاوی اطلاعات واقعی دیتابیس و کلیدهای امنیتی شماست و نباید جایگزین یا
   حذف شود.**
۲. کل محتوای فعلی پوشهٔ `navracar-app/` **به‌جز فایل `.env`** را حذف کنید.
۳. `navracar-app-update.zip` را در همان مسیر (`navracar-app/`) آپلود و
   Extract کنید.
۴. مطمئن شوید فایل `.env` قدیمی‌تان هنوز سر جایش هست (چون در مرحلهٔ ۱ حذفش
   نکردید).

### مرحلهٔ ۳: جایگزینی فایل‌های عمومی
محتوای پوشهٔ `public_html/` (فایل‌های قبلی `index.php`, `build/`, `.htaccess`,
`robots.txt`, `favicon.ico`) را با محتوای `public_html-update.zip` جایگزین
کنید. اگر فایل `setup-once.php` هنوز آنجا مانده، همان‌جا نگه دارید یا حذف کنید
— در این بسته وجود ندارد چون لازم نیست.

### مرحلهٔ ۴: بررسی نهایی
۱. `https://navracar.com` را باز کنید و مطمئن شوید صفحهٔ اصلی، محاسبه‌گر
   (`/calculator`) و یک صفحهٔ آگهی خودرو درست باز می‌شوند.
۲. روی یک آگهی خودرو، دکمهٔ «درخواست پیش‌فاکتور» را بزنید، فرم را پر کنید و
   مطمئن شوید لینک دانلود PDF ظاهر و فایل واقعاً باز می‌شود.
۳. از پنل مدیریت، «پیش‌فاکتورها و فروش» ← یک پیش‌فاکتور ← «دانلود فایل PDF» را
   هم امتحان کنید.

## نکتهٔ فنی مهم دربارهٔ فونت PDF
برای این‌که فایل‌های PDF متن فارسی را درست نشان دهند، یک فونت فارسی/عربی
(Noto Naskh Arabic — رایگان و متن‌باز، لایسنس SIL OFL) داخل کد بسته‌بندی شده و
از پوشهٔ `storage/fonts/` به‌عنوان کش موقت دومپی‌دی‌اف استفاده می‌شود. این پوشه
باید توسط PHP قابل نوشتن باشد؛ روی اکثر هاست‌های اشتراکی این پیش‌فرض برقرار
است و کاری لازم نیست، اما اگر هنگام درخواست پیش‌فاکتور خطای «Permission denied»
دیدید، دسترسی نوشتن پوشهٔ `navracar-app/storage` (و زیرپوشه‌هایش) را به ۷۵۵
تغییر دهید.

## این بسته قبل از تحویل واقعاً تست شد
یک محیط کامل شبیه‌سازی‌شده (همان آرایش `navracar-app/` + `public_html/` کنار
هم، روی دیتابیس MySQL واقعی migrate‌شده) ساخته و کل مسیرها بررسی شدند: صفحهٔ
اصلی، محاسبه‌گر، صفحهٔ آگهی، وبلاگ، sitemap.xml، و — مهم‌تر از همه — **درخواست
واقعی پیش‌فاکتور از صفحهٔ آگهی زده شد و فایل PDF واقعی تولید، دانلود، و باز
شد** (تأیید شد که یک PDF معتبر با متن فارسی درست‌رندرشده است).
