# به‌روزرسانی ناوراکار — رفع باگ صدور پیش‌فاکتور + محدودسازی دسترسی نقش‌ها

باز هم یک **به‌روزرسانی جزئی (فقط فایل‌های تغییریافته)** است — بدون لمس
دیتابیس، بدون نیاز به `vendor/`. این‌بار هم چون رنگی به تِم اضافه نشده اما
تغییرات ظاهری (مثل تقسیم‌بندی نو منوی پنل مدیریت) رخ داده، فایل CSS دوباره
عوض شده — پس باز هم پوشهٔ `build/` باید در `public_html/` جایگزین شود.

## چه چیزهایی در این نسخه درست/اضافه شد

1. **رفع باگ ۵۰۰ صدور پیش‌فاکتور** — وقتی کارشناس فروش یک پیش‌فاکتور
   *جدید* (بدون اتصال به یک درخواست موجود) صادر می‌کرد و «ثبت و مشاهده» را
   می‌زد، سایت خطای ۵۰۰ می‌داد. علت: مقدار پنهان «شناسهٔ درخواست» به‌جای
   خالی، عدد ۰ ارسال می‌شد و چون هیچ درخواستی با شناسهٔ ۰ وجود ندارد، قید
   کلید خارجی دیتابیس نقض و کل عملیات با خطا متوقف می‌شد. این باگ کاملاً
   رفع شد.
2. **جمع کل پیش‌فاکتور حالا واقعاً زنده محاسبه می‌شود** — قبلاً فیلد «جمع
   کل قبل از تخفیف» یک باکس کاملاً دستی و بی‌ربط به ردیف‌های هزینه بود
   (همان چیزی که گفتید محاسبه نمی‌کند). حالا این فیلد خودکار و آنی از روی
   جمع تمام ردیف‌های اقلام محاسبه می‌شود و قابل ویرایش دستی نیست (تا کسی
   اشتباهاً عددی نامرتبط با ردیف‌ها وارد نکند).
3. **بخش ارزش‌افزوده** — یک تیک «افزودن مالیات بر ارزش افزوده» با درصد
   قابل‌تنظیم (پیش‌فرض ۱۰٪) اضافه شد؛ با فعال‌کردن آن، یک ردیف مالیات
   خودکار به لیست اقلام اضافه و در جمع کل لحاظ می‌شود.
4. **حالت «محاسبهٔ خودکار» در فرم صدور پیش‌فاکتور** — دقیقاً مثل صفحهٔ
   محاسبه‌گر سایت: قیمت خودرو (درهم)، دسته‌بندی، و نرخ ارز را وارد کنید،
   همان فرمول کامل عوارض گمرکی/پلاک محاسبه می‌شود، و با یک دکمه می‌توانید
   نتیجه را جایگزین ردیف‌های دستی کنید (بعدش هم قابل ویرایش دستی هستند).
5. **واحد پول دلار** — علاوه بر تومان و درهم، حالا «دلار (USD)» هم به‌عنوان
   واحد صدور پیش‌فاکتور قابل انتخاب است.
6. **محدودسازی دسترسی «مدیر محتوا»** — این نقش از این پس **فقط** به چهار
   بخش دسترسی دارد: آگهی‌های دابیزل، وبلاگ، اسلایدر صفحهٔ اصلی، و منوی
   سایت. دیگر نمی‌تواند وارد داشبورد، کانبان، درخواست‌ها، پیش‌فاکتورها،
   محاسبات، شماره‌شاسی‌ها، خروجی اکسل، لاگ سیستم، تنظیمات یا کاربران شود.
7. **محدودسازی دسترسی «کارشناس فروش»** — این نقش از این پس **فقط** به
   «درخواست‌ها»، «پایپ‌لاین (کانبان)» و «پیش‌فاکتورها» دسترسی دارد، و
   داخل همین بخش‌ها هم **فقط درخواست‌ها/پیش‌فاکتورهای الحاق‌شده به خودش**
   را می‌بیند و می‌تواند ویرایش کند — نه همهٔ سرنخ‌ها و پیش‌فاکتورهای سایر
   همکاران. مدیر کامل مثل قبل به همه‌چیز دسترسی دارد.
8. بعد از ورود، هر نقش به صفحهٔ متناسب با خودش هدایت می‌شود: مدیر →
   داشبورد، کارشناس فروش → لیست درخواست‌ها، مدیر محتوا → آگهی‌های دابیزل.

## فایل این بسته
- `navracar-app-update.zip` — فایل‌های تغییریافته (کنترلرها، مدل‌ها،
  میان‌افزار جدید نقش فروش، Blade، مسیرها) + پوشهٔ `public/build/` تازه.

## مراحل به‌روزرسانی
۱. `navracar-app-update.zip` را داخل `navracar-app/` آپلود و Extract کنید،
   «Overwrite» را بزنید (فقط فایل‌های داخل بسته جایگزین می‌شوند؛ `.env`،
   `vendor/`، `storage/` و دیتابیس دست‌نخورده می‌مانند).
۲. داخل `public_html/`، پوشهٔ `build/` فعلی را کامل حذف کنید، سپس پوشهٔ
   `build/` را از داخل `navracar-app/public/build/` (که در مرحلهٔ ۱
   به‌روز شد) کپی و مستقیم در `public_html/` جای‌گذاری کنید — نتیجه باید
   `public_html/build/manifest.json` باشد (نه `public_html/public/build/`).
۳. تست سریع: آدرس `https://navracar.com/build/manifest.json` را باز کنید؛
   باید یک متن کوتاه JSON ببینید، نه خطای ۴۰۴.
۴. `https://navracar.com` را با Hard Refresh (Ctrl+Shift+R) باز کنید.

## بررسی نهایی
۱. با یک حساب کارشناس فروش وارد شوید، یک پیش‌فاکتور جدید (بدون انتخاب از
   یک درخواست) بسازید، چند ردیف هزینه اضافه کنید و مطمئن شوید «جمع کل»
   خودش زنده محاسبه می‌شود، و «ثبت و مشاهده» بدون خطا کار می‌کند.
۲. تیک «افزودن مالیات بر ارزش افزوده» را بزنید و ببینید ردیف و جمع کل
   به‌روز می‌شود.
۳. بخش «محاسبهٔ خودکار هزینه‌ها» را باز کنید، قیمت درهم را وارد کنید، و
   «جایگزینی اقلام» را بزنید — ردیف‌ها باید پر شوند.
۴. با یک حساب مدیر محتوا وارد شوید و مطمئن شوید فقط ۴ بخش محتوایی را در
   منوی کناری می‌بیند (نه کانبان/درخواست‌ها/پیش‌فاکتورها/تنظیمات و...).
۵. با یک حساب کارشناس فروش دیگر وارد شوید و مطمئن شوید پیش‌فاکتور/درخواست
   کارشناس اول را نمی‌بیند (اگر مستقیم لینکش را هم باز کند، پیام «دسترسی
   غیرمجاز» می‌بیند).

## این بسته قبل از تحویل واقعاً تست شد
سناریوی «سایت شما همین الان» شبیه‌سازی شد (نسخهٔ بستهٔ قبلی، روی MySQL
واقعی migrate‌شده، سپس همین فایل‌ها رویش اعمال شد) و با مرورگر واقعی
(Chromium/Playwright) تأیید شد:
- ورود با حساب کارشناس فروش → مستقیم به لیست درخواست‌ها هدایت شد.
- ساخت پیش‌فاکتور جدید بدون اتصال به درخواست، با محاسبهٔ زندهٔ جمع کل، و
  ثبت موفق **بدون خطای ۵۰۰** — دقیقاً همان جریانی که قبلاً خراب بود.
- ورود با حساب مدیر محتوا → تلاش برای باز کردن داشبورد و لیست پیش‌فاکتورها
  هر دو با پاسخ ۴۰۳ (دسترسی غیرمجاز) مسدود شدند، دقیقاً طبق خواستهٔ شما.
