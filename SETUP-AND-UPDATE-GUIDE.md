# ناوراکار — راهنمای نصب و آپدیت (بر اساس آخرین نسخه)

این راهنما دقیقاً با فایل‌هایی که همین الان در پکیج هست منطبق است. اگر نسخه قبلی‌تری نصب کرده بودید،
بخش «۳) آپدیت از نسخه قبلی» را دنبال کنید؛ اگر نصب تازه است، بخش «۲) نصب تازه».

---

## ۱) فهرست کامل فایل‌ها

```
/  (پوشه اصلی سایت شما)
│
├── ناوراکار-محاسبه‌گر-واردات-خودرو.html   محاسبه‌گر اصلی (ویزارد ۵ مرحله‌ای)
├── .htaccess                              امنیت + سرعت (فشرده‌سازی، کش، مسدودسازی فایل حساس)
│
├── ajax-bootstrap.php     پایه مشترک همه فرم‌های آنلاین (الزامی — کنار db-config.php)
├── db-config.php          مشخصات دیتابیس (از قبل با اطلاعات واقعی شما پر شده)
├── debug-log.php          سیستم لاگ فعالیت/خطا (باید کنار db-config.php باشد، نه داخل admin/)
├── geo-helper.php         تشخیص کشور/شهر از روی IP
├── create-admin.php       ساخت حساب مدیر (بعد از استفاده حذف شود)
│
├── send-quote.php         ثبت «درخواست استعلام قیمت» از داخل محاسبه‌گر
├── log-calculation.php    ثبت خاموش هر محاسبه‌ای که کاربر انجام می‌دهد
├── log-vin.php            ثبت هر استعلام شماره شاسی
│
├── lead-form.php          فرم عمومی ثبت تماس فروش (بدون لاگین، لینک برای فروشنده‌ها)
├── submit-lead-form.php   دریافت‌کننده همان فرم
│
├── schema.sql             ساختار کامل دیتابیس — فقط برای نصب کاملاً تازه
├── schema-update.sql      ارتقا ۱: پایه CRM (الحاق، وضعیت پیگیری)
├── schema-update-2.sql    ارتقا ۲: فرم عمومی (بودجه، تاریخ تماس بعدی، ثبت‌کننده)
├── schema-update-3.sql    ارتقا ۳: ایندکس سرعت + ستون تخفیف پیش‌فاکتور
├── schema-update-4.sql    ارتقا ۴: پیش‌فاکتور کامل (ایمیل مشتری، ارز، اعتبار، شرایط پرداخت، نوع پیش‌فاکتور)
├── schema-crm-v2.sql      ارتقا ۵: پایپ‌لاین کانبان + قالب‌های پیام + دمای سرنخ
│
└── admin/                 پنل مدیریت
    ├── auth-check.php, style.php, partials-header.php   پایه مشترک پنل
    ├── login.php, logout.php                            ورود/خروج (با لاگ و قفل موقت بعد از ۶ تلاش ناموفق)
    ├── index.php              داشبورد (KPI، نمودار ۱۴ روزه، سرنخ‌های داغ، تماس‌های امروز)
    ├── kanban.php             پایپ‌لاین فروش به‌صورت کانبان (درگ‌اند‌دراپ)
    ├── change-stage.php       endpoint تغییر مرحله (برای کانبان)
    ├── requests.php           لیست درخواست‌ها (نمای جدولی، فیلتر و جستجو)
    ├── request-view.php       جزئیات سرنخ: الحاق، پیگیری، دما، قالب پیام آماده با کپی
    ├── lead-new.php           ثبت دستی مشتری تماس‌گرفته (AJAX با پیام موفقیت)
    ├── templates.php          مدیریت قالب‌های پیام (فقط مدیر)
    ├── log-template-use.php   ثبت استفاده از قالب در تاریخچه سرنخ
    ├── calculations.php       محاسبات ثبت‌شده روی سایت
    ├── vin-checks.php         استعلام‌های شماره شاسی
    ├── users.php              مدیریت کاربران پنل (فقط مدیر)
    ├── invoices.php           لیست پیش‌فاکتورها
    ├── invoice-new.php        صدور/ویرایش پیش‌فاکتور (کامل یا تک‌آیتمی، تومان/درهم)
    ├── invoice-view.php       نمایش/چاپ پیش‌فاکتور
    ├── export.php             خروجی اکسل
    └── activity-log.php       لاگ فعالیت سایت (فقط مدیر — شامل ورود/خروج پنل هم می‌شود)
```

**نکته مهم:** `debug-log.php` و `ajax-bootstrap.php` باید دقیقاً کنار `db-config.php` در پوشه اصلی باشند،
نه داخل `admin/`. اگر جای این‌ها اشتباه باشد، کل سایت با خطا مواجه می‌شود.

---

## ۲) نصب تازه

1. یک دیتابیس MySQL بسازید و کاربرش را با دسترسی کامل بدهید.
2. فایل `schema.sql` را در phpMyAdmin روی آن دیتابیس Import کنید (همه جدول‌ها یک‌جا ساخته می‌شوند).
3. اگر مشخصات دیتابیستان با آنچه در `db-config.php` نوشته شده فرق دارد، همان سه خط بالای فایل را ویرایش کنید.
4. یک‌بار در مرورگر باز کنید: `https://yourdomain.com/create-admin.php?u=USERNAME&p=PASSWORD&role=admin&name=نام شما`
5. بلافاصله بعد، `create-admin.php` را از روی سرور حذف کنید.
6. همه فایل‌ها را (یا با آپلود مستقیم پوشه، یا با آپلود zip و Extract در File Manager) در همان ساختار پوشه‌بندی آپلود کنید.
7. وارد `admin/login.php` شوید.

---

## ۳) آپدیت از نسخه قبلی

### مرحله الف — پشتیبان‌گیری
از phpMyAdmin یک Export کامل از دیتابیس بگیرید، و یک zip از پوشه فعلی سایت هم از File Manager دانلود کنید.

### مرحله ب — بررسی اینکه کدام ارتقاها را قبلاً اجرا کرده‌اید
در تب SQL دیتابیستان، این کوئری‌ها را یکی‌یکی اجرا کنید. هرکدام **نتیجه خالی** داد، فایل SQL روبه‌رویش را
Import کنید (تب Import در phpMyAdmin). اگر نتیجه داد، همان مرحله را قبلاً انجام داده‌اید و رد شوید.

| کوئری بررسی | اگر خالی بود → اجرا کنید |
|---|---|
| `SHOW COLUMNS FROM quote_requests LIKE 'assigned_to';` | `schema-update.sql` |
| `SHOW COLUMNS FROM quote_requests LIKE 'budget_range';` | `schema-update-2.sql` |
| `SHOW COLUMNS FROM quote_requests LIKE 'next_call_date';` و `SHOW COLUMNS FROM invoices LIKE 'discount_amount';` | `schema-update-3.sql` |
| `SHOW COLUMNS FROM invoices LIKE 'currency';` | `schema-update-4.sql` |
| `SHOW COLUMNS FROM quote_requests LIKE 'temperature';` | `schema-crm-v2.sql` |

این فایل‌ها را **به همین ترتیب** اجرا کنید (هرکدام به ستون‌های فایل قبلی نیاز دارد). هیچ‌کدام داده موجود را پاک نمی‌کند.
اگر پیام «Duplicate column» گرفتید یعنی آن بخش را قبلاً اجرا کرده‌اید — بی‌خطر است، فایل بعدی را ادامه دهید.

### مرحله ج — جایگزینی فایل‌ها
ساده‌ترین و مطمئن‌ترین راه: **همه فایل‌های جدول بالا را رونویسی (Overwrite) کنید.** فایل‌به‌فایل تصمیم نگیرید
کدام عوض شده — همه‌شان هماهنگ با هم بازسازی شده‌اند و هیچ‌کدام داده‌ای حذف نمی‌کند.

---

## ۴) چک‌لیست تست بعد از نصب/آپدیت

| تست | نتیجه مورد انتظار |
|---|---|
| باز کردن فایل html اصلی، رفتن تا انتهای ویزارد | عدد جمع کل و گزارش چاپی درست نمایش داده شود |
| مرحله آخر ویزارد یا مسیر «ارتباط مستقیم» | هر سه شماره (ایران، امارات، دفتر تهران) با دکمه‌های تماس/واتساپ/تلگرام جدا دیده شود؛ نباید بشود به فضای خالی سمت چپ اسکرول کرد |
| فرم «درخواست استعلام دقیق» در انتهای ویزارد | پیام موفقیت |
| باز کردن `lead-form.php` روی موبایل واقعی و ثبت یک مورد | پیام سبز موفقیت؛ نباید صفحه بعد از تایپ در فیلدها به چپ کشیده شود |
| ورود به `admin/login.php` | داشبورد باز شود |
| «لاگ سیستم» (فقط مدیر) | باید همین ورود شما را با `[INFO]` ببینید |
| خروج از پنل و ورود دوباره | هر دو رویداد در لاگ سیستم ثبت شوند |
| «پایپ‌لاین (کانبان)» | ستون‌های مرحله دیده شود، یک کارت را بکشید و جابه‌جا کنید |
| «قالب‌های پیام» (فقط مدیر) → افزودن قالب → صفحه جزئیات یک سرنخ → انتخاب قالب → «کپی متن» | متن به‌درستی خوانا باشد (نه سفید روی سفید) و در کلیپ‌بورد کپی شود |
| «+ ثبت دستی مشتری تماس‌گرفته» | فرم کامل (شامل بودجه با بازه‌های جدید) و پیام موفقیت + دکمه «ثبت مشتری جدید» |
| صدور یک پیش‌فاکتور کامل از روی یک درخواست موجود | ردیف «سود خدمات ناوراکار» هم به‌صورت جدا در اقلام دیده شود |
| صدور یک پیش‌فاکتور تک‌آیتمی (مثلاً فقط «صدور مجوز») | فقط همان یک ردیف، بدون بخش خودرو |
| در فرم پیش‌فاکتور، واحد پول را «درهم» کنید و نرخ ارز بدهید | در صفحه پیش‌فاکتور، معادل تومانی هم نمایش داده شود |
| از صفحه پیش‌فاکتور روی «ویرایش» بزنید | فرم با همه اطلاعات قبلی پر شده باشد و بشود ذخیره کرد |
| اضافه کردن مبلغ تخفیف | «مبلغ قابل‌پرداخت» درست کم شود |
| ورود با یک حساب «کارشناس فروش» | فقط فرم‌های الحاق‌شده به همان کارشناس در لیست/کانبان دیده شود |

اگر چیزی کار نکرد، همیشه اول به **«لاگ سیستم»** در پنل مدیریت سر بزنید — نام فایل، شماره خط، و پیام خطا آنجا نوشته می‌شود.

---

## ۵) امنیت
- `create-admin.php` را بلافاصله بعد از ساخت حساب‌های لازم حذف کنید.
- `db-config.php`، `debug-log.php`، `ajax-bootstrap.php`، `geo-helper.php` مستقیم از مرورگر باز نمی‌شوند — طبیعی و درست است.
- بعد از ۶ تلاش ناموفق ورود، پنل برای ۵ دقیقه قفل می‌شود.
- کوکی نشست با `HttpOnly` و `SameSite` محافظت می‌شود؛ روی HTTPS خودکار `Secure` هم فعال می‌شود.
- فرم عمومی فروش و فرم استعلام قیمت، محافظت خاموش دربرابر ربات دارند (بدون کپچا، کاربر واقعی چیزی حس نمی‌کند).
- `.htaccess` دسترسی مستقیم به فایل‌های `.sql`/`.log`/تنظیمات را مسدود می‌کند و هدرهای امنیتی اضافه می‌کند.

## ۶) شماره‌های تماس شرکت (در همه گزارش‌ها/صفحات درج شده)
- 🇮🇷 +98 912 051 2149 — واتساپ | بله | تلگرام
- 🇦🇪 +971 50 515 8484 — واتساپ | تلگرام
- ☎️ +98 21 8887 0878 — دفتر تهران
