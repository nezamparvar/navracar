{
  "name": "navra-capture-extension",
  "version": "1.1.0",
  "description": "Navra Capture Browser Extension for NavraCar",
  "scripts": {
    "build": "npm run bundle && npm run archive:staging && npm run archive:production && npm run checksum",
    "archive:staging": "cd dist/staging && zip -qr ../navra-capture-staging.zip .",
    "archive:production": "cd dist/production && zip -qr ../navra-capture-production.zip .",
    "checksum": "node scripts/checksum.js",
    "bundle": "node scripts/bundle.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src/**/*.js src/**/*.ts",
    "dev": "npm run bundle && echo 'Extension bundled. Load from dist/staging'"
  },
  "keywords": [
    "chrome",
    "extension",
    "vehicle",
    "navracar"
  ],
  "author": "Navra",
  "license": "PROPRIETARY",
  "devDependencies": {
    "@types/chrome": "^0.0.252",
    "@types/jest": "^30.0.0",
    "@types/node": "^26.2.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.4.1",
    "ts-jest": "^29.4.12",
    "typescript": "^5.2.2"
  }
}
