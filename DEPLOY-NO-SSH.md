# نصب ناوراکار (لاراول) روی هاست بدون SSH

این راهنما برای هاستی نوشته شده که فقط **File Manager** و **phpMyAdmin** دارید (بدون
ترمینال/SSH). چون این نسخه دیگر PHP ساده نیست و لاراول است، `composer`، `npm` و
`php artisan` لازم دارد — این‌ها همه از قبل روی یک بستهٔ آماده اجرا شده‌اند تا شما
فقط چند فایل را آپلود و پیکربندی کنید.

## چیزهایی که تحویل می‌گیرید
- `navracar-deploy.zip` — کل کد برنامه به‌همراه `vendor/` و فایل‌های build‌شدهٔ CSS/JS
  (کاملاً آماده اجراست، نیازی به composer/npm نیست).
- `public_html-files.zip` — نسخهٔ آمادهٔ `public_html` (برای روش B پایین).
- `database-import.sql` — کل ساختار دیتابیس + داده‌های اولیه، برای Import در phpMyAdmin
  (نیازی به `php artisan migrate` نیست).
- `env-production.txt` — نمونهٔ فایل `.env` با یک `APP_KEY` واقعی از قبل ساخته‌شده.
- `setup-once.php` — صفحهٔ نصب یک‌بارمصرف که کارهایی را انجام می‌دهد که معمولاً
  `php artisan` انجام می‌دهد (ساخت لینک عکس‌ها، ساخت حساب مدیر).

## مرحله ۱: ساخت دیتابیس (مثل قبل)
از cPanel → **MySQL Databases** یک دیتابیس و یک کاربر با دسترسی کامل بسازید (دقیقاً
مثل قبل). نام دیتابیس/یوزر/پسورد را یادداشت کنید.

## مرحله ۲: Import ساختار دیتابیس
در **phpMyAdmin**، دیتابیسی که ساختید را باز کنید → تب **Import** → فایل
`database-import.sql` را انتخاب و Import کنید. همهٔ جدول‌ها + داده‌های اولیهٔ CRM
(مراحل پایپ‌لاین، قالب‌های پیام و...) یک‌جا ساخته می‌شوند.

## مرحله ۳: آپلود فایل‌ها

لاراول باید طوری اجرا شود که فقط پوشهٔ `public/` آن از اینترنت در دسترس باشد (نه کل
کد). دو روش وجود دارد — **روش A را اول امتحان کنید**؛ اگر هاستتان اجازه نداد، روش B
همیشه کار می‌کند.

### روش A (اگر هاست اجازهٔ تغییر Document Root را می‌دهد) — ساده‌تر
1. `navracar-deploy.zip` را در File Manager آپلود کنید (مثلاً کنار `public_html`، در
   یک پوشهٔ جدید به اسم `navracar-app`) و Extract کنید.
2. در cPanel بروید به **Domains** → دامنهتان → **Document Root** را عوض کنید به
   `navracar-app/public`.
3. همین — دیگر نیازی به `public_html-files.zip` یا اصلاح `index.php` نیست، چون
   `navracar-app/public/index.php` مسیرهای اصلی خودش را دارد.

### روش B (اگر نمی‌توانید Document Root را عوض کنید) — همیشه کار می‌کند
1. `navracar-deploy.zip` را آپلود و Extract کنید، طوری که یک پوشهٔ **کنار** (هم‌سطح،
   نه داخل) `public_html` به اسم دقیقاً `navracar-app` داشته باشید:
   ```
   /home/شما/
     ├── public_html/        (همینی که الان هست)
     └── navracar-app/       (از navracar-deploy.zip)
   ```
2. `public_html-files.zip` را باز کنید و **محتوای** آن (`index.php`, `setup-once.php`,
   `build/`, `.htaccess`, `favicon.ico`, `robots.txt`) را مستقیم داخل `public_html`
   بریزید (فایل‌های قدیمی سایت را قبلش پاک یا به یک پوشهٔ بک‌آپ منتقل کنید).
   این نسخه از `index.php` از قبل طوری اصلاح شده که به‌جای `navracar-app/`، مسیر درست
   کنار `public_html` را پیدا کند.
3. اگر اسم پوشه یا محلش را عوض کردید، خط `$appBase = __DIR__.'/../navracar-app';`
   را داخل `index.php` و `setup-once.php` با مسیر واقعی خودتان جایگزین کنید.

## مرحله ۴: تنظیم `.env`
محتوای `env-production.txt` را در یک فایل جدید به اسم `.env` در ریشهٔ `navracar-app`
(کنار `composer.json`، **نه** داخل `public_html`) کپی کنید و این‌ها را با مقادیر
واقعی جایگزین کنید:
- `DB_DATABASE` / `DB_USERNAME` / `DB_PASSWORD` — از مرحلهٔ ۱
- `APP_URL` — آدرس واقعی سایت
- `SETUP_TOKEN` — یک رشتهٔ طولانی و تصادفی دلخواه (مثلاً از یک سایت تولید پسورد)

`APP_KEY` از قبل یک مقدار واقعی و امن دارد، نیازی به تغییر نیست.

## مرحله ۵: ساخت حساب مدیر + لینک عکس‌ها
در مرورگر باز کنید:
```
https://سایت‌شما/setup-once.php?token=همان SETUP_TOKEN که در .env گذاشتید
```
یک فرم می‌بینید: نام کاربری و رمز عبور مدیر را وارد و ثبت کنید. همین صفحه لینک
`public/storage` را هم برای نمایش عکس‌های دانلودشده از دابیزل می‌سازد.

## مرحله ۶: پاک‌سازی امنیتی (حتماً انجام دهید)
بعد از این‌که با موفقیت وارد `https://سایت‌شما/admin/login` شدید:
1. خط `SETUP_TOKEN=...` را از `.env` پاک کنید.
2. فایل `setup-once.php` را کامل از `public_html` حذف کنید.

## اگر صفحه سفید یا خطای 500 دیدید
- مطمئن شوید نسخهٔ PHP روی هاست **۸.۲ یا بالاتر** است (cPanel → Select PHP Version).
- در File Manager، مسیر `navracar-app/storage/logs/laravel.log` را باز کنید — دقیق‌ترین
  پیام خطا آنجاست.
- پوشه‌های `navracar-app/storage` و `navracar-app/bootstrap/cache` باید قابل‌نوشتن
  باشند (در File Manager روی هرکدام راست‌کلیک → Permissions → ۷۵۵).

## به‌روزرسانی‌های بعدی
هر وقت تغییر جدیدی آماده شد، یا فقط چند فایل تغییریافته را جایگزین می‌کنید، یا (اگر
جدول دیتابیس جدیدی اضافه شده) یک فایل `.sql` جدید برای Import در phpMyAdmin دریافت
می‌کنید — دقیقاً به همین روش، بدون نیاز به SSH.
