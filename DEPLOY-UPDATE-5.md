# به‌روزرسانی ناوراکار — رفع باگ منوی موبایل + راهنمای رفع مشکل جایگذاری فایل‌ها

## بخش ۱: رفع مشکلی که گفتید — «منوها در نسخه موبایلی دیده نمی‌شوند»

این یک باگ واقعی در کد بود، نه مشکل تنظیمات: در نسخهٔ موبایل (صفحهٔ گوشی)،
لینک‌های منو («محاسبه قیمت خودرو»، «قیمت خودروها»، و آیتم‌های منوی پنل)
اصلاً کدی برای نمایش روی موبایل نداشتند — فقط در نسخهٔ دسکتاپ دیده می‌شدند.
حالا یک **دکمهٔ همبرگری (☰)** کنار لوگو در نسخهٔ موبایل اضافه شده که با لمس
آن، منو به‌صورت یک پنل کشویی زیر هدر باز می‌شود و همهٔ لینک‌ها آنجا دیده
می‌شوند.

این تغییر باعث شد فایل CSS سایت هم دوباره کمی تغییر کند — پس **باز هم باید
پوشهٔ `build/` را در `public_html/` جایگزین کنید**، دقیقاً مثل دفعهٔ قبل.
چون گفتید در این کار به مشکل خوردید، این‌بار مراحل را با جزئیات بیشتر و یک
راه تست ساده می‌نویسم.

## بخش ۲: دربارهٔ مشکلی که پیش آمد (جایگذاری اشتباه پوشهٔ public)

از توضیح شما به نظر می‌رسد پوشهٔ `public/` (که داخل بستهٔ قبلی، زیرِ
`navracar-app/` قرار می‌گیرد) به‌جای اینکه فقط **محتوای زیرپوشهٔ `build/`
آن** به `public_html/` منتقل شود، به شکل یک پوشهٔ تودرتو اضافه در
`public_html/` کپی شده — یعنی به‌جای این ساختار درست:

```
public_html/
├── index.php          (از قبل بود، دست‌نخورده)
├── build/
│   ├── manifest.json
│   └── assets/...
```

این ساختار غلط ایجاد شده:

```
public_html/
├── index.php
├── public/                 ← اشتباه: یک پوشهٔ اضافه به نام «public»
│   └── build/
│       ├── manifest.json
│       └── assets/...
```

وقتی این اتفاق بیفتد، مرورگر دنبال فایل‌های CSS/JS در آدرس
`navracar.com/build/assets/...` می‌گردد ولی فایل واقعی در
`navracar.com/public/build/assets/...` است — پس فایل‌ها پیدا نمی‌شوند و
سایت بدون طراحی (فقط متن ساده، بدون رنگ و چیدمان) یا خراب دیده می‌شود.

### راه رفع (در File Manager هاست)
۱. وارد `public_html/` شوید.
۲. اگر پوشه‌ای به نام `public` آنجا می‌بینید (که نباید باشد)، وارد آن شوید
   و ببینید آیا `public/build/` وجود دارد.
۳. آن زیرپوشهٔ `build/` را **Cut/Move** کنید و مستقیماً داخل `public_html/`
   (یک پله بالاتر، بیرون از پوشهٔ `public`) بگذارید — طوری که آدرس نهایی
   بشود `public_html/build/manifest.json`.
۴. حالا پوشهٔ خالی‌شدهٔ `public_html/public/` را کامل حذف کنید (این پوشه
   نباید اصلاً داخل `public_html/` وجود داشته باشد).
۵. مطمئن شوید کنار `build/`، فایل‌های اصلی قبلی هم هنوز سر جایشان هستند:
   `index.php`, `.htaccess`, `robots.txt`, `favicon.ico`.

### راه تست سریع (بدون نیاز به دانش فنی)
در مرورگر این آدرس را باز کنید (به‌جای `...` نام یکی از فایل‌های داخل
`build/assets/` را از File Manager نگاه کنید و جایگزین کنید — یا ساده‌تر،
همین آدرس زیر را امتحان کنید):

```
https://navracar.com/build/manifest.json
```

- اگر یک متن کوتاه شبیه کد (JSON، با علامت‌های `{` و `}`) دیدید → فایل‌ها
  سر جای درستشان هستند، مشکل حل شده.
- اگر پیام «۴۰۴ Not Found» یا صفحهٔ خطا دیدید → هنوز فایل‌ها در مسیر درست
  نیستند، مرحلهٔ بالا را دوباره چک کنید.

## فایل‌های این بسته
- `navracar-app-update.zip` — شامل یک فایل Blade تغییریافته
  (`resources/views/components/layouts/public.blade.php`) و پوشهٔ
  `public/build/` تازه‌کامپایل‌شده.

## مراحل به‌روزرسانی (این‌بار قدم‌به‌قدم‌تر)

### مرحلهٔ ۱: navracar-app/
۱. `navracar-app-update.zip` را داخل پوشهٔ `navracar-app/` آپلود کنید.
۲. Extract کنید، «Overwrite» را بزنید.
   - نتیجه باید این باشد: فایل
     `navracar-app/resources/views/components/layouts/public.blade.php`
     به‌روز شده، و پوشهٔ `navracar-app/public/build/` هم با فایل‌های تازه
     جایگزین شده.

### مرحلهٔ ۲: public_html/ — فقط پوشهٔ build را منتقل کنید
۱. به `navracar-app/public/build/` بروید (همان‌جایی که در مرحلهٔ ۱ آپدیت
   شد).
۲. **کل پوشهٔ `build`** (نه محتوایش تک‌تک، خودِ پوشه) را کپی کنید.
۳. به `public_html/` بروید. اگر از قبل پوشه‌ای به نام `build` آنجا هست،
   کاملاً حذفش کنید.
۴. پوشهٔ `build` که در مرحلهٔ ۲ کپی کردید را همین‌جا (مستقیم داخل
   `public_html/`, نه داخل زیرپوشهٔ دیگری) Paste کنید.
۵. با راه تست سریع بالا (`navracar.com/build/manifest.json`) مطمئن شوید
   جای درستی قرار گرفته.

⚠️ **نکتهٔ مهم**: پوشهٔ `public/` که داخل `navracar-app/` است، **هرگز نباید**
با همین نام «public» به `public_html/` منتقل شود — فقط زیرپوشهٔ `build/`
داخل آن باید به `public_html/` برود، آن‌هم مستقیم (بدون پوشهٔ واسط).

### مرحلهٔ ۳: بررسی نهایی
۱. `https://navracar.com` را با Hard Refresh (Ctrl+Shift+R) باز کنید.
۲. گوشی موبایل (یا حالت موبایل مرورگر، F12 → آیکون گوشی) را باز کنید،
   وارد `/car-prices` شوید و روی آیکون ☰ کنار لوگو بزنید — باید منو باز
   شود و لینک‌ها (محاسبه قیمت خودرو، قیمت خودروها، و در صورت وجود آیتم‌های
   دلخواه) دیده شوند.

## این بسته قبل از تحویل واقعاً تست شد
سناریوی «سایت شما همین الان» دوباره شبیه‌سازی شد (نسخهٔ قبلی روی MySQL
واقعی، سپس همین فایل‌ها رویش اعمال شد) و با مرورگر واقعی (Chromium/
Playwright) در اندازهٔ صفحهٔ موبایل (۳۹۰ پیکسل عرض) تأیید شد که با زدن
دکمهٔ همبرگری، منو باز می‌شود و همهٔ لینک‌ها (شامل یک آیتم منوی دلخواه که
از پنل اضافه شده بود) به‌درستی دیده می‌شوند — بدون هیچ خطای جاوااسکریپت.
