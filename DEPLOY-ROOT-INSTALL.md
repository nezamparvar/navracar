# نصب ناوراکار در ریشه دامنه (navracar.com/) — بدون SSH

این راهنما مخصوص نصب **در ریشهٔ اصلی دامنه** است (نه زیرشاخهٔ `/list/`) — برای وقتی
که `public_html` هنوز خالی است و می‌خواهید ناوراکار مستقیم روی `navracar.com`
بالا بیاید. مراحل دقیقاً مثل نصب قبلی (زیرشاخهٔ `/list/`) است، فقط ساختار پوشه‌ها
یک پله ساده‌تر می‌شود چون دیگر نیازی به شاخهٔ میانی نیست.

فایل‌های این نصب (تحویل داده‌شده جدا از این مخزن گیت):
- `navracar-app.zip` — کل کد لاراول (شامل vendor/ آمادهٔ تولید و public/build/ برای
  استفادهٔ داخلی خود لاراول — این پوشه هرگز مستقیم از وب در دسترس نیست).
- `public_html-contents.zip` — فایل‌هایی که باید مستقیم داخل `public_html` قرار
  بگیرند (`index.php` از قبل برای این آرایش پوشه تنظیم شده).
- `database-import-root.sql` — کل دیتابیس (جدول‌ها + دیتای اولیهٔ لازم مثل
  مراحل پایپ‌لاین و قالب‌های پیام)، آماده ایمپورت در phpMyAdmin.
- `env-production-root.txt` — نمونهٔ فایل `.env` با `APP_KEY` و `SETUP_TOKEN`
  از پیش تولیدشده (نیازی به ساختن دستی نیست، فقط اطلاعات دیتابیس را وارد کنید).

## مرحله ۱: ساخت دیتابیس (cPanel → MySQL Databases)

یک دیتابیس و یک کاربر بسازید (مثلاً `navrac_main` و `navrac_main`)، رمز قوی
بسازید، و کاربر را با «All Privileges» به دیتابیس متصل کنید. این سه مقدار
(نام دیتابیس، نام کاربری، رمز) را یادداشت کنید — الان لازمشان داریم.

## مرحله ۲: ایمپورت دیتابیس (phpMyAdmin)

دیتابیسی که ساختید را باز کنید ← تب Import ← فایل `database-import-root.sql`
را انتخاب و Go بزنید. باید حدود ۲۲ جدول ساخته شود (از جمله یک کاربر مدیر
پیش‌فرض با نام کاربری `admin` و رمز `password` — **حتماً بلافاصله بعد از اولین
ورود این رمز را عوض کنید** یا این کاربر را حذف و از مرحله ۵ یک کاربر واقعی
بسازید).

## مرحله ۳: آپلود فایل‌ها (File Manager)

۱. به‌جای `public_html`، یک پوشه **کنار** آن بسازید (هم‌سطح، نه داخلش) با نام
   `navracar-app` — یعنی اگر مسیر `public_html` چیزی شبیه `/home/USERNAME/public_html`
   است، پوشهٔ جدید باید `/home/USERNAME/navracar-app` باشد.
۲. `navracar-app.zip` را همان‌جا آپلود و Extract کنید (محتوایش باید مستقیم
   داخل `navracar-app/` باز شود، نه در یک زیرپوشهٔ تودرتو دیگر).
۳. حالا وارد `public_html` شوید. اگر فایل/پوشهٔ قدیمی‌ای آنجا نیست (طبق گفتهٔ
   شما، این دامنه هنوز محتوایی ندارد)، مستقیم `public_html-contents.zip` را
   همان‌جا آپلود و Extract کنید.

ساختار نهایی باید این شکلی باشد:
```
/home/USERNAME/
├── navracar-app/       (کد اصلی لاراول — هرگز مستقیم از وب باز نمی‌شود)
└── public_html/        (ریشهٔ وب navracar.com — همان چیزی که در مرورگر می‌بینید)
    ├── index.php
    ├── build/
    ├── setup-once.php
    └── robots.txt
```

## مرحله ۴: ساخت فایل .env

داخل `navracar-app/`، فایل `env-production-root.txt` را که آپلود کرده‌اید باز
کنید (یا از کامپیوتر خودتان ویرایش و دوباره آپلود کنید)، این سه خط را با
اطلاعات واقعی مرحلهٔ ۱ پر کنید:
```
DB_DATABASE=نام‌دیتابیس‌شما
DB_USERNAME=نام‌کاربری‌شما
DB_PASSWORD=رمز‌شما
```
اگر رمز دیتابیس با علامت `#` شروع می‌شود، حتماً بین `"..."` بگذارید (مثال:
`DB_PASSWORD="#Ab12cd34"`) — وگرنه لاراول همه‌چیز بعد از `#` را نادیده می‌گیرد و
اتصال با خطای رمز خالی مواجه می‌شود (یک باگ واقعی که در نصب قبلی همین مشکل را
داد).

بعد این فایل را در File Manager به نام دقیق `.env` (با نقطهٔ ابتدا، بدون
`.txt`) داخل پوشهٔ `navracar-app/` ذخیره کنید. برای دیدن/ساختن فایل‌هایی که با
نقطه شروع می‌شوند، از منوی Settings بالای File Manager گزینهٔ «Show Hidden
Files» را فعال کنید.

## مرحله ۵: اجرای نصب یک‌بارمصرف

آدرس زیر را باز کنید (توکن از پیش در `env-production-root.txt` ساخته شده،
همان مقدار را اینجا هم بگذارید):

```
https://navracar.com/setup-once.php?token=1bf13ba156ecbf434028efcc7b9bee33e4e80022dbb81e6c
```

این صفحه یک فرم ساخت مدیر نشان می‌دهد — نام کاربری و رمز واقعی خودتان را وارد
کنید و «ساخت / به‌روزرسانی حساب مدیر» را بزنید. همین صفحه به‌طور خودکار لینک
`storage` را هم می‌سازد (برای نمایش عکس‌های دانلودشده از دابیزل).

## مرحله ۶: ورود و پاک‌سازی امنیتی

به `https://navracar.com/admin/login` بروید و با حساب تازه‌ساخته وارد شوید.
بعد از ورود موفق:
۱. اگر از کاربر پیش‌فرض seed (`admin`/`password`) هم استفاده نکردید، از پنل
   («کاربران») آن را حذف یا رمزش را عوض کنید.
۲. از فایل `.env` خط `SETUP_TOKEN=...` را کامل پاک کنید.
۳. فایل `public_html/setup-once.php` را کامل از سرور حذف کنید.

## مرحله ۷: تنظیمات اولیهٔ پنل

به «تنظیمات نرخ ارز» بروید و نرخ ارز آزاد/گمرک روز، شماره‌های تماس، و در
صورت تمایل توکن ربات تلگرام/بله را وارد کنید (بخش «ربات‌های انتشار محتوا»).

## نکات فنی که همین حالا رعایت شده (لازم نیست خودتان انجام دهید)

- نسخهٔ production پوشهٔ `vendor/` از قبل با `composer install --no-dev` ساخته
  شده — نیازی به Composer روی هاست نیست.
- فایل‌های `resources/css`/`resources/js` از قبل با Vite build شده‌اند
  (`public/build/` و `public_html/build/` — هر دو نسخهٔ یکسان، یکی برای استفادهٔ
  داخلی لاراول و دیگری برای سرو مستقیم به مرورگر).
- `bootstrap/cache/packages.php` و `services.php` عمداً حذف شده‌اند تا لاراول
  آن‌ها را با vendor واقعی سرور دوباره بسازد (نسخهٔ dev این فایل‌ها به پکیج‌های
  فقط-توسعه اشاره می‌کند که در `vendor/` تولیدی وجود ندارند).
- **این بستهٔ نصب کامل قبل از تحویل، با شبیه‌سازی دقیق همین آرایش پوشه‌ها (روی
  یک سرور PHP واقعی، با migrate واقعی) تست شد** — صفحهٔ اصلی، محاسبه‌گر، قیمت
  خودروها، وبلاگ، sitemap.xml، ورود ادمین، و بارگذاری فایل‌های CSS/JS همگی با
  موفقیت ۲۰۰ برگرداندند.
