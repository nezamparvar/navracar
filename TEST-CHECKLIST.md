# چک‌لیست تست کامل ناوراکار

این سند برای اجرای تست توسط یک عامل/ابزار دیگر (مثلاً Codex) نوشته شده — همهٔ
ویژگی‌هایی که در این نشست ساخته شده‌اند را پوشش می‌دهد. مراحل به‌ترتیب اولویت
(از راه‌اندازی محیط تا تست تک‌تک فیچرها) چیده شده‌اند.

## ۱. راه‌اندازی محیط تست

```bash
composer install
npm install && npm run build
cp .env.example .env   # اگر .env از قبل نیست
php artisan key:generate
touch database/database.sqlite
# در .env: DB_CONNECTION=sqlite ، DB_DATABASE=<مسیر مطلق database/database.sqlite>
php artisan migrate --seed
php artisan serve
```

کاربر ادمین پیش‌فرض (seeder، فقط در APP_ENV=local کار می‌کند): `admin` / `password`

## ۲. اجرای تست‌های خودکار (این خط را حتماً اول اجرا کن)

```bash
php artisan test
./vendor/bin/pint --test
```

انتظار: همهٔ تست‌ها سبز (در زمان نوشتن این سند: ۱۷ تست، همگی PASS)، و Pint بدون
خطای استایل. اگر تستی قرمز شد، قبل از هر تست دستی دیگر، همین‌جا اول باگ را پیدا
و رفع کن.

## ۳. تست دستی — بخش عمومی سایت (بدون لاگین)

### ۳.۱ صفحه اصلی (`/`)
- [ ] بارگذاری بدون خطا؛ اسلایدر هیرو (اگر اسلاید فعالی در دیتابیس هست) نمایش
      داده می‌شود.
- [ ] بلافاصله بعد از اسلایدر، جعبهٔ CTA زرد «محاسبه آنلاین هزینه واردات خودرو»
      دیده می‌شود و به `/calculator` لینک می‌دهد.
- [ ] اگر آگهی منتشرشده وجود دارد، بخش «آخرین خودروهای موجود» کارت‌ها را نشان
      می‌دهد؛ کلیک هر کارت به صفحهٔ همان خودرو می‌رود.
- [ ] بخش «مرور خودروها بر اساس دسته و بازه قیمت» چیپ‌های دسته موتور + بازه
      قیمت را نشان می‌دهد؛ هرکدام باید به صفحهٔ فیلترشدهٔ درست برود.
- [ ] اگر پست وبلاگ منتشرشده هست، پیش‌نمایش وبلاگ نمایش داده می‌شود.
- [ ] فوتر: شماره‌های تماس (واتساپ ایران/امارات، دفتر تهران) و لینک‌های سریع
      (صفحه اصلی/محاسبه‌گر/قیمت خودروها/آیتم‌های منوی سفارشی) درست‌اند.
- [ ] هدر سایت: دکمهٔ زرد «محاسبه قیمت خودرو» + دکمهٔ «قیمت خودروها» + هر آیتم
      منوی فعال (از پنل ادمین اضافه‌شده) دیده می‌شوند.
- [ ] View Source: تگ `<script type="application/ld+json">` حاوی
      `Organization` و `WebSite` وجود دارد و JSON معتبر است (با
      `JSON.parse` در کنسول مرورگر تست کن).

### ۳.۲ محاسبه‌گر (`/calculator`)
- [ ] صفحه با موفقیت لود می‌شود (این همان صفحهٔ اصلی قبلی است، فقط آدرسش عوض
      شده).
- [ ] شماره‌های تماس (بخش «ارتباط مستقیم با کارشناسان» و بخش «۲۹ - Contact»
      و متن پاورقی/پیش‌فاکتور چاپی) دقیقاً همان مقادیری‌اند که در پنل مدیریت →
      تنظیمات → «شماره‌های تماس سایت» ثبت شده — یک شماره را در پنل عوض کن و
      دوباره این صفحه را لود کن تا مطمئن شوی زنده می‌خواند (نه هاردکد).
- [ ] ویزارد را تا مرحلهٔ «خودروی خود را انتخاب کنید» جلو ببر، در باکس جستجو
      نام یک برندی که در «قیمت خودروها» آگهی منتشرشده دارد را تایپ و انتخاب کن
      (مثلاً اگر آگهی Toyota هست، «Toyota» را انتخاب کن).
      - [ ] باکس «آگهی‌های آماده این برند در ناوراکار» زیر بخش انتخاب نسخه/حجم
            موتور ظاهر می‌شود و کارت(های) آگهی مرتبط را با عکس/عنوان/قیمت نشان
            می‌دهد.
      - [ ] کلیک روی کارت به صفحهٔ همان آگهی در `/car-prices/{slug}` می‌رود
            (تب جدید).
      - [ ] اگر برندی انتخاب کنی که آگهی‌ای برایش نیست، این باکس مخفی می‌ماند
            (نه خالی/خطا).
- [ ] کل مسیر محاسبه (وارد کردن قیمت، انتخاب دسته، دیدن جدول هزینه) هنوز مثل
      قبل درست کار می‌کند — این فیچر قدیمی نباید خراب شده باشد.

### ۳.۳ قیمت خودروها (`/car-prices`)
- [ ] لیست آگهی‌های منتشرشده نمایش داده می‌شود؛ چیپ‌های «برند» / «دسته موتور»
      / «بازه قیمت» بالای لیست دیده می‌شوند.
- [ ] کلیک روی یک چیپ برند → می‌رود به `/car-prices/brand/{make}` و فقط
      آگهی‌های همان برند را نشان می‌دهد؛ breadcrumb و عنوان صفحه با آن برند
      هماهنگ است.
- [ ] همین برای چیپ دسته موتور (`/car-prices/category/{id}`) و بازه قیمت
      (`/car-prices/price/{bracket}`) — هرکدام باید فقط نتایج درست را نشان
      دهند.
- [ ] آدرس نامعتبر مثل `/car-prices/category/not-a-real-id` باید ۴۰۴ بدهد.
- [ ] View Source هر سه نوع صفحه: JSON-LD نوع `BreadcrumbList` و `ItemList`
      موجود و معتبر است.

### ۳.۴ صفحهٔ یک آگهی خودرو (`/car-prices/{slug}`)
- [ ] عکس‌ها، عنوان، قیمت درهم + معادل تومانی، «مدت زمان تحویل تخمینی: N روز
      کاری» (اگر برای آن آگهی مقدار دارد)، مشخصات فنی، دکمه‌های واتساپ (اماره/
      ایران، لینک صحیح wa.me با شماره‌های تنظیمات).
- [ ] جدول کامل محاسبهٔ هزینه:
      - ردیف «عوارض گمرکی بر اساس تعرفه» (نه «سود بازرگانی») با درصد درست دسته.
      - ردیف‌های «حمل دریایی» و «هزینه صدور مجوزهای واردات» دیده می‌شوند (قبلاً
        دیده نمی‌شدند — این یک باگ رفع‌شده است).
      - ردیف «گواهی اسقاط خودرو فرسوده» با تعداد گواهی و رتبهٔ انرژی صحیح از
        تنظیمات دسته‌بندی.
      - تغییر مقادیر ورودی (نرخ ارز، قیمت، دسته) جدول را زنده به‌روز می‌کند.
- [ ] دکمهٔ «درخواست پیش‌فاکتور» → پاپ‌آپ نام/تلفن/ایمیل باز می‌شود؛ ارسال با
      نام+تلفن معتبر باید پیام موفقیت نشان دهد و یک رکورد در پنل ادمین →
      «درخواست‌ها» ثبت شود (بررسی کن).
- [ ] JSON-LD نوع `Vehicle` (با `vehicleTransmission`, `bodyType`,
      `vehicleEngine` در صورت وجود دیتا) و `BreadcrumbList` موجود و معتبرند.

### ۳.۵ وبلاگ (`/blog`, `/blog/{slug}`)
- [ ] لیست پست‌های منتشرشده؛ پست پیش‌نویس در این لیست دیده نمی‌شود.
- [ ] باز کردن مستقیم آدرس یک پست پیش‌نویس (بدون لاگین) باید ۴۰۴ بدهد.
- [ ] صفحهٔ یک پست منتشرشده: عکس کاور (اگر هست)، عنوان، تاریخ، متن کامل؛
      JSON-LD نوع `BlogPosting` معتبر است.

### ۳.۶ Sitemap و robots.txt
- [ ] `GET /sitemap.xml` باید XML معتبر با این‌ها برگرداند: آدرس صفحه اصلی،
      `/calculator`، `/car-prices`، `/blog`، همهٔ دسته‌های موتور، همهٔ بازه‌های
      قیمت، همهٔ برندهای موجود، همهٔ آگهی‌های منتشرشده، همهٔ پست‌های منتشرشده.
- [ ] `GET /robots.txt` باید به `https://<دامنه>/sitemap.xml` اشاره کند.

### ۳.۷ فرم عمومی ثبت سرنخ (`/lead-form`)
- [ ] فیلد «کشور» وجود دارد (پیش‌فرض «ایران»)؛ با تغییر کشور به غیر از ایران،
      فیلد «شهر» از یک لیست کشویی به یک اینپوت متنی آزاد تغییر می‌کند.
- [ ] ثبت موفق فرم با یک کشور غیر ایرانی، رکورد را با آن کشور/شهر آزاد در پنل
      ادمین ذخیره می‌کند.

## ۴. تست دستی — پنل مدیریت

برای هر بخش، هم با کاربر «مدیر» (`admin`) و هم با یک کاربر «مدیر محتوا»
(`content_manager`) و یک کاربر «کارشناس فروش» (`sales`) تست کن مگر خلافش گفته
شده باشد.

### ۴.۱ نقش‌ها و دسترسی
- [ ] در پنل «کاربران»، یک کاربر جدید با نقش «مدیر محتوا» بساز.
- [ ] با این کاربر لاگین کن: باید «آگهی‌های دابیزل»، «وبلاگ»، «اسلایدر صفحه
      اصلی»، «منوی سایت» را در منوی کناری ببیند و بتواند وارد هرکدام شود.
- [ ] همین کاربر نباید بتواند به `/admin/settings` یا `/admin/users` برود
      (باید ۴۰۳ بگیرد).
- [ ] یک کاربر «کارشناس فروش» بساز و لاگین کن: نباید هیچ‌کدام از موارد بالا
      (نه محتوا، نه تنظیمات) را ببیند یا به آدرسشان مستقیم دسترسی داشته باشد.

### ۴.۲ آگهی‌های دابیزل — دریافت خودکار (فیچر قدیمی)
- [ ] از فرم بالای صفحه، یک لینک دابیزل واقعی وارد کن (یا HTML صفحه را پیست
      کن) → باید آگهی پیش‌نویس بسازد و به صفحهٔ ویرایش ببرد.

### ۴.۳ آگهی خودرو — افزودن دستی (فیچر جدید)
- [ ] دکمهٔ «افزودن آگهی دستی (بدون دابیزل)» → فرم کامل (بدون فیلد لینک منبع)
      باز می‌شود.
- [ ] پر کردن عنوان + قیمت + بقیهٔ فیلدها و ذخیره → آگهی پیش‌نویس با
      `source_site = manual` ساخته می‌شود، اسلاگ خودکار تولید می‌شود (اگر خالی
      گذاشته شود)، و به صفحهٔ ویرایش می‌رود.
- [ ] در صفحهٔ ویرایش این آگهی، دکمهٔ «بازخوانی از دابیزل» نباید دیده شود
      (چون منبعی ندارد).
- [ ] در لیست آگهی‌ها، این ردیف باید نشان «دستی» (برچسب آبی) داشته باشد.

### ۴.۴ آگهی خودرو — ایمپورت گروهی (فیچر جدید، برای خروجی کرالر)
- [ ] یک فایل JSON نمونه بساز (آرایه‌ای با ۲-۳ آبجکت، هرکدام حداقل
      `source_url` و `title_en`/`price_aed` — به شکل خروجی
      `tools/navracar-crawler/navracar_crawler.py`).
- [ ] از صفحهٔ لیست آگهی‌ها → «ایمپورت گروهی از فایل کرالر» → آپلود همان فایل.
- [ ] پیام موفقیت باید تعداد «جدید»، «تکراری (رد شد)»، «ناموفق» را نشان دهد.
- [ ] همان فایل را دوباره آپلود کن — این‌بار همه باید «تکراری» باشند (نه
      دوباره ساخته شوند).
- [ ] آگهی‌های تازه‌ساخته را در لیست پیدا و باز کن، مطمئن شو فیلدها درست پر
      شده‌اند.

### ۴.۵ دسته‌بندی و تعرفه (فیچر مرحلهٔ قبل، دوباره چک شود)
- [ ] در ویرایش یک آگهی، دراپ‌داون «دسته‌بندی خودرو» باید ۸ گزینه داشته باشد:
      برقی، پلاگین هیبرید، هیبرید، و ۵ بازهٔ سی‌سی بنزینی — با درصد تعرفهٔ کنار
      هرکدام که از تنظیمات خوانده می‌شود.

### ۴.۶ انتشار در شبکه‌های اجتماعی (فیچر جدید)
- [ ] در تنظیمات پنل → «ربات‌های انتشار محتوا»، توکن یک ربات تست تلگرام و
      شناسه یک کانال/چت تست وارد کن (اگر ربات واقعی نداری، از یک توکن نامعتبر
      استفاده کن تا مسیر خطا را هم ببینی).
- [ ] در ویرایش یک آگهی که حداقل یک عکس دارد، کارت «انتشار در شبکه‌های
      اجتماعی» را پیدا کن:
      - [ ] دکمهٔ «ارسال به تلگرام»: با توکن نامعتبر پیام خطای قرمز نشان دهد؛
            با توکن/چت واقعی (اگر داری) باید پیام سبز موفقیت بدهد و واقعاً در
            کانال تلگرام پست شود (عکس + کپشن + لینک + هشتگ‌ها).
      - [ ] دکمهٔ «ارسال به بله»: مشابه بالا.
      - [ ] دکمهٔ «اشتراک در واتساپ»: باید در تب جدید واتساپ‌وب/اپ را با متن
            آماده (عنوان+قیمت+توضیح+لینک+هشتگ) باز کند؛ عکس پیوست نمی‌شود (این
            محدودیت شناخته‌شده و مستندشدهٔ API رایگان واتساپ است، نه باگ).
- [ ] روی یک آگهی بدون عکس، دکمه‌های تلگرام/بله باید بلافاصله خطای «این مورد
      عکسی ندارد» بدهند (نه تلاش برای ارسال).
- [ ] همین سه دکمه را در صفحهٔ ویرایش یک پست وبلاگ (که عکس کاور دارد) هم تست
      کن.

### ۴.۷ اسلایدر صفحه اصلی (فیچر جدید)
- [ ] افزودن اسلاید جدید با عکس/عنوان/زیرعنوان/متن دکمه/لینک دکمه → باید در
      صفحهٔ اصلی سایت (پس از رفرش) دیده شود.
- [ ] ویرایش این‌جایی فیلدهای یک اسلاید (بدون آپلود عکس جدید) → ذخیره می‌شود و
      عکس قبلی دست‌نخورده می‌ماند.
- [ ] غیرفعال‌کردن یک اسلاید → از صفحهٔ اصلی محو می‌شود (ولی حذف نمی‌شود).
- [ ] حذف یک اسلاید → هم رکورد هم فایل عکسش پاک می‌شود.

### ۴.۸ منوی سایت (فیچر جدید)
- [ ] افزودن آیتم منو (عنوان + لینک داخلی مثل `/blog` یا لینک خارجی کامل) →
      باید در هدر و فوتر همهٔ صفحات عمومی (به‌جز `/calculator` که layout جدا
      دارد) دیده شود.
- [ ] «تب جدید» تیک‌خورده باید `target="_blank"` تولید کند.
- [ ] غیرفعال/حذف کردن آیتم → از سایت محو می‌شود.

### ۴.۹ وبلاگ ادمین (فیچر جدید)
- [ ] «نوشتن مطلب جدید» → عنوان+خلاصه+متن (HTML ساده مثل `<p>`، `<b>` مجاز
      است) + عکس کاور → ذخیره به‌صورت پیش‌نویس.
- [ ] دکمهٔ «انتشار در سایت» → وضعیت به «منتشرشده» تغییر می‌کند و در `/blog`
      دیده می‌شود.
- [ ] «لغو انتشار» → دوباره پیش‌نویس و از سایت مخفی می‌شود.
- [ ] حذف پست → رکورد و عکس کاورش پاک می‌شوند.

### ۴.۱۰ تنظیمات پنل (گسترش‌یافته)
تک‌تک این کارت‌ها را باز کن، مقدار عوض کن، ذخیره کن، و مطمئن شو مقدار جدید هم
در پنل هم در سایت عمومی منعکس می‌شود:
- [ ] نرخ ارز (آزاد/گمرک)
- [ ] هزینه‌های ثابت (صدور مجوز، حمل دریایی، انبارداری، مدت تحویل پیش‌فرض)
- [ ] گواهی اسقاط (نرخ هر گواهی، آستانهٔ قیمت)
- [ ] جدول دسته‌بندی و تعرفه (۸ ردیف — درصد + رتبهٔ انرژی هرکدام)
- [ ] شماره‌های تماس سایت (واتساپ ایران/امارات، دفتر تهران) — سه‌جا را چک کن:
      صفحهٔ محاسبه‌گر، صفحهٔ هر آگهی خودرو، و پیش‌فاکتور چاپی
      (`/admin/invoices/{id}`).
- [ ] ربات‌های تلگرام/بله (توکن + شناسه چت)

### ۴.۱۱ درخواست‌ها / CRM (فیلد کشور جدید)
- [ ] «ثبت دستی مشتری» → فیلد «کشور» را به چیزی غیر از «ایران» تغییر بده →
      فیلد «شهر» باید از دراپ‌داون به اینپوت متنی آزاد تبدیل شود.
- [ ] بعد از ثبت، جزئیات همان مشتری در پنل باید کشور/شهر واردشده را نشان دهد.

## ۵. تست ابزار کرالر دسکتاپ (اختیاری — نیاز به پایتون و اتصال اینترنت واقعی)

```bash
cd tools/navracar-crawler
python3 -m py_compile navracar_crawler.py   # فقط سینتکس، بدون شبکه
```

اگر به dubizzle.com دسترسی شبکه داری:
```bash
cp config.example.json config.json
# داخل config.json حداقل یک دسته با max_pages_per_category: 1 و
# max_listings_per_category: 2 بگذار تا تست سریع باشد
python3 navracar_crawler.py --config config.json
```
- [ ] یک فایل `navracar-import-*.json` ساخته می‌شود.
- [ ] محتوای آن یک آرایهٔ JSON با فیلدهای `source_url`, `title_en`,
      `price_aed`, `images` (آرایه‌ای از URLهای `dbz-images.dubizzle.com`) است.
- [ ] همین فایل را در بخش ۴.۴ بالا (ایمپورت گروهی پنل) آپلود کن — باید بدون
      خطا آگهی واقعی بسازد.

## ۶. رگرسیون نهایی

- [ ] `php artisan test` دوباره اجرا شود — باید هنوز همه سبز باشند.
- [ ] هیچ خطای PHP در `storage/logs/laravel.log` طی کل این تست‌ها ثبت نشده
      باشد (فایل را در پایان بررسی کن).
- [ ] کنسول مرورگر (DevTools) در هیچ‌کدام از صفحات عمومی/ادمین بالا خطای JS
      قرمز نداشته باشد.
