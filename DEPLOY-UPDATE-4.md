# به‌روزرسانی ناوراکار — نسخهٔ جدید (اصلاح سقف وام، قیمت ریالی، وام چندساله، چارت زمان‌بندی)

این هم مثل بستهٔ قبلی یک **به‌روزرسانی جزئی (فقط فایل‌های تغییریافته)** است، نه
نصب از صفر. باز هم هیچ پکیج PHP جدیدی اضافه نشده، پس نیازی به دست‌زدن به
`vendor/` نیست. اما این‌بار برخلاف دفعهٔ قبل، **چون یک رنگ جدید به تِم سایت
اضافه شده، فایل CSS کامپایل‌شده هم تغییر کرده** — بنابراین این‌بار باید هم
داخل `navracar-app/` و هم داخل `public_html/` چند فایل را جایگزین کنید (هر دو
داخل همین یک بستهٔ zip هستند). نیازی به ایمپورت مجدد دیتابیس نیست.

## چه چیزهایی در این نسخه درست/اضافه شد

1. **اصلاح سقف وام**: سقف وام حالا دقیقاً طبق قاعدهٔ اعلامی شما محاسبه
   می‌شود — کمترین مقدار از «سقف وام مصوب پنل مدیریت» و «۵۰٪ قیمت
   تمام‌شدهٔ نهایی همان خودرو» (نه قیمت خام خودرو). مثال: قیمت تمام‌شدهٔ
   ۱۵ میلیارد ← سقف وام ۷.۵ میلیارد؛ قیمت تمام‌شدهٔ ۳۰ میلیارد ← سقف وام
   همان ۱۰ میلیارد (سقف مصوب) خواهد بود، چون ۵۰٪ آن (۱۵ میلیارد) از سقف
   مصوب بیشتر است.
2. **نمایش قیمت تمام‌شده به ریال**: روی کارت‌های صفحهٔ فهرست «قیمت
   خودروها» (و صفحات فیلترشدهٔ برند/دسته/بازهٔ قیمت)، زیر قیمت درهم هر
   خودرو، قیمت تمام‌شدهٔ نهایی به ریال هم نمایش داده می‌شود.
3. **شرایط وام چندساله**: از پنل «تنظیمات» ← «تنظیمات وام و اقساط» حالا
   می‌توانید چند مدت بازپرداخت را هم‌زمان فعال کنید (مثلاً `2,3,5` برای
   وام‌های ۲، ۳ و ۵ ساله) — در جدول اقساط صفحهٔ هر خودرو، هر مدت به‌صورت
   یک ردیف جداگانه (با قسط ماهانه و جمع بازپرداخت خودش) نمایش داده
   می‌شود.
4. **چارت زمان‌بندی پرداخت‌ها و انجام کار**: یک نمودار افقی تازه در پایین
   جدول شرایط پرداخت هر آگهی خودرو اضافه شد — هم مبلغ هر مرحله و هم
   مدت‌زمان تقریبی آن را با طول متناسب نشان می‌دهد (بوک کردن، صدور مجوز،
   باقی‌ماندهٔ قیمت، ترخیص گمرکی، کارمزد کارگزار، پلاک انتظامی) و در پایان
   با علامت ✓ «تحویل خودرو ترخیص‌شده و دارای پلاک انتظامی» را نشان می‌دهد.

## فایل این بسته
- `navracar-app-update.zip` — شامل هم فایل‌های کد تغییریافته (کنترلرها،
  مدل، Blade) و هم پوشهٔ `public/build/` تازه‌کامپایل‌شده (چون رنگ جدید
  اضافه شده). این فایل باید هم در `navracar-app/` و هم در `public_html/`
  (فقط پوشهٔ `build/` داخلش) اعمال شود — مراحل دقیق پایین آمده.

## مراحل به‌روزرسانی

### مرحلهٔ ۱: پشتیبان‌گیری (احتیاطی)
از پوشه‌های `app/`, `resources/` و `public/build/` داخل `navracar-app/`
فعلی‌تان یک نسخهٔ فشرده در File Manager بگیرید.

### مرحلهٔ ۲: به‌روزرسانی کد داخل navracar-app/
۱. `navracar-app-update.zip` را در **داخل پوشهٔ `navracar-app/`** آپلود
   کنید.
۲. Extract کنید و وقتی می‌پرسد فایل‌های هم‌نام جایگزین شوند یا نه،
   «Overwrite / جایگزینی» را بزنید. این کار فایل‌های زیر را به‌روز می‌کند:
   چند فایل داخل `app/`, چند فایل Blade داخل `resources/`, و کل پوشهٔ
   `public/build/` (شامل CSS/JS تازه) — به `.env`, `vendor/`, `storage/`
   یا دیتابیس دست نمی‌زند.

### مرحلهٔ ۳: به‌روزرسانی پوشهٔ build/ داخل public_html/
چون فایل CSS سایت عوض شده، باید نسخهٔ آن در `public_html/` هم به‌روز شود:
۱. داخل `public_html/`، پوشهٔ `build/` فعلی را کامل حذف کنید.
۲. پوشهٔ `build/` را از داخل همین بسته (که در `navracar-app/public/build/`
   هم اکسترکت کردید) کپی کنید و در `public_html/` با همان نام `build`
   جای‌گذاری کنید — یعنی نتیجه باید `public_html/build/manifest.json` و
   `public_html/build/assets/...` باشد. (بقیهٔ فایل‌های `public_html/` مثل
   `index.php`, `.htaccess`, `robots.txt`, `favicon.ico` دست‌نخورده
   می‌مانند، لازم نیست جایگزین شوند.)

### مرحلهٔ ۴: بررسی نهایی
۱. `https://navracar.com` را با Hard Refresh (Ctrl+Shift+R) باز کنید تا
   نسخهٔ قدیمی CSS از کش مرورگر پاک شود.
۲. یک صفحهٔ آگهی خودرو را باز کنید — پایین جدول «شرایط و مراحل پرداخت»
   باید «چارت زمان‌بندی پرداخت‌ها و انجام کار» را ببینید (نواری رنگی با
   ۶ بخش، رنگ هرچه به چپ می‌رود تیره‌تر).
۳. بالای همان صفحه، «جدول اقساط وام» باید حالا چند ردیف («وام ۲ ساله»،
   «وام ۳ ساله»، «وام ۵ ساله» — بسته به تنظیم پنل) نشان دهد.
۴. صفحهٔ «قیمت خودروها» را باز کنید — زیر قیمت درهم هر خودرو باید خط
   «قیمت تمام‌شده: ... ریال» را ببینید.
۵. اگر می‌خواهید شرایط وام چندساله را تغییر دهید: پنل مدیریت ← تنظیمات ←
   «تنظیمات وام و اقساط» ← فیلد «مدت‌های بازپرداخت (سال)» — اعداد را با
   کاما جدا بنویسید، مثلاً `2,3,5`.

## این بسته قبل از تحویل واقعاً تست شد
دوباره دقیقاً سناریوی «سایت شما همین الان» شبیه‌سازی شد: نسخهٔ بستهٔ
**قبلی** (که همین الان روی سایت شماست) روی دیتابیس MySQL واقعی migrate شد،
سپس همین فایل‌ها — هم کد و هم `build/` جدید، در هر دو پوشهٔ `navracar-app`
و `public_html` — رویش اعمال شدند و:
- صفحهٔ فهرست قیمت خودروها با خط «قیمت تمام‌شده: ... ریال» درست باز شد.
- در صفحهٔ یک خودرو با قیمت گران، سقف وام دقیقاً روی سقف مصوب پنل (۱۰
  میلیارد) قفل شد چون ۵۰٪ قیمت تمام‌شده از آن بیشتر بود — و در یک خودروی
  ارزان‌تر، وام دقیقاً ۵۰٪ قیمت تمام‌شده محاسبه شد.
- جدول اقساط سه ردیف (۲، ۳، ۵ ساله) درست با مبالغ متفاوت نمایش داده شد.
- چارت زمان‌بندی با مرورگر واقعی (Chromium/Playwright) رندر و بدون خطای
  کنسول تأیید شد — هم در حالت روشن، هم تاریک، هم موبایل.
