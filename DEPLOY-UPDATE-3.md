# به‌روزرسانی ناوراکار — نسخهٔ جدید (دلار، جدول اقساط/پرداخت، اصلاح فونت PDF)

این یک **بستهٔ به‌روزرسانی** است، نه نصب از صفر — فرض بر این است که سایت شما از
قبل روی `navracar.com` نصب و در حال کار است و یک پوشهٔ `navracar-app/` کنار
`public_html/` دارید.

**نیازی به ایمپورت مجدد دیتابیس یا اجرای دوبارهٔ `setup-once.php` نیست** — این
نسخه هیچ جدول یا ستون جدیدی به دیتابیس اضافه نکرده؛ فقط کد برنامه عوض شده
(تنظیمات جدید مثل نرخ دلار و پارامترهای وام به‌صورت خودکار با مقدار پیش‌فرض کار
می‌کنند و از پنل «تنظیمات» قابل تغییرند).

## چه چیزهایی در این نسخه درست/اضافه شد

1. **واحد دلار در محاسبه‌گر**: در محاسبه‌گر اصلی سایت (`/calculator`) و در
   جدول محاسبهٔ هزینهٔ هر آگهی خودرو، کنار فیلدهای «قیمت واقعی خودرو» و «قیمت
   گمرکی خودرو» یک دکمهٔ دوتایی «درهم / دلار» اضافه شد — کاربرانی که نرخ درهم
   را نمی‌دانند می‌توانند مبلغ را مستقیم دلاری وارد کنند (تبدیل خودکار بر
   اساس نرخ دلار به درهم که از پنل «تنظیمات» قابل تنظیم است).
2. **اصلاح فونت فارسی PDF پیش‌فاکتور**: فونت قبلی (نستعلیق سنتی) با فونت
   **Noto Sans Arabic** جایگزین شد که خوانایی آن به‌مراتب بهتر و نزدیک به فونت
   اصلی سایت است.
3. **دکمهٔ «محاسبه مجدد»**: در جدول هزینهٔ واردات صفحهٔ هر آگهی خودرو اضافه شد
   — اگر کاربر عددی را دستی تغییر دهد، با زدن این دکمه یک تأیید بصری می‌بیند
   که محاسبه به‌روز شده (محاسبه همیشه زنده است؛ این دکمه صرفاً بازخورد بصری
   می‌دهد).
4. **تغییر نام «سود خدمات ناوراکار»** در تمام سایت (محاسبه‌گر، صفحهٔ آگهی
   خودرو، پنل مدیریت، فایل CSV خروجی، PDF پیش‌فاکتور) به **«کارمزد ترخیص‌کار و
   کارگزار (ناوراکار)»**.
5. **برچسب بازهٔ قیمتی خودروها** (زیر ۵ میلیارد، ۵ تا ۱۰ میلیارد و...) دیگر
   بر اساس قیمت خام درهم خودرو نیست؛ بر اساس **قیمت تمام‌شدهٔ واقعی** (بعد از
   محاسبهٔ کامل عوارض گمرکی، حمل، و پلاک) محاسبه می‌شود — دقیق‌تر و واقعی‌تر.
6. **قانون مرزبندی رده‌های سی‌سی** بازبینی و با یک تست خودکار قفل شد: خودروی
   دقیقاً ۲۵۰۰ سی‌سی در رده «۲۰۰۰ تا ۲۵۰۰» قرار می‌گیرد (نه رده بالاتر)؛ فقط
   خودروی **بیش از** ۲۵۰۰ سی‌سی وارد رده «۲۵۰۰ تا ۳۰۰۰» می‌شود. (بررسی نشان داد
   این منطق از قبل درست پیاده شده بود، فقط با تست رگرسیون تثبیت شد.)
7. **جدول اقساط وام خرید خارجی خودرو** — در پایین جدول هزینهٔ واردات هر آگهی
   خودرو اضافه شد:
   - سقف وام، مدت بازپرداخت (پیش‌فرض ۵ سال) و نرخ بهرهٔ سالانه (پیش‌فرض ۲۸٪)
     از پنل «تنظیمات» ← «تنظیمات وام و اقساط» قابل تغییرند.
   - این وام صرفاً بابت بخشی از **هزینه‌های ترخیص گمرکی** همان خودرو محاسبه
     می‌شود، نه کل قیمت خودرو.
   - برای خودروهای ارزان‌تر، سقف وام حداکثر «۵۰٪ قیمت خودرو» است (حتی اگر
     کمتر از سقف کلی وام باشد) — مطابق قاعدهٔ اعلامی: خودروی ۱۲ میلیارد
     تومانی حداکثر ۶ میلیارد (۵۰٪) وام می‌گیرد چون سقف ۵۰٪ قبل از سقف کلی ۱۰
     میلیارد محدودکننده است.
   - مبلغ قسط ماهانه، تعداد اقساط، جمع کل بازپرداخت، و **پیش‌پرداخت لازم**
     نمایش داده می‌شوند، به‌همراه یادآوری که برای استفاده از وام باید «فرم
     درخواست خرید خودروی خارجی با وام» تکمیل شود.
8. **جدول شرایط و مراحل پرداخت** — دقیقاً زیر جدول اقساط، با ۶ مرحلهٔ دقیق و
   مبلغ واقعی هر مرحله برای همان خودرو:
   ۱) ۱۰٪ قیمت خودرو برای بوک کردن (۱ روز) ۲) هزینهٔ صدور مجوز (حدود ۱ هفته)
   ۳) الباقی قیمت خودرو در امارات (ارسال به ایران معمولاً ۳ روز) ۴) جمع هزینه‌های
   ترخیص خودرو (معمولاً ۲۰ تا ۴۰ روز کاری) ۵) کارمزد ترخیص‌کار و کارگزار
   ۶) هزینهٔ پلاک انتظامی.

## فایل‌های این بسته
- `navracar-app-update.zip` — کد به‌روزشدهٔ لاراول + `vendor/` + فونت فارسی
  جدید (Noto Sans Arabic).
- `public_html-update.zip` — فایل‌های به‌روزشدهٔ `public_html` (شامل CSS/JS
  جدید Vite).

## مراحل به‌روزرسانی

### مرحلهٔ ۱: پشتیبان‌گیری (احتیاطی)
از پوشهٔ `navracar-app/` فعلی‌تان یک نسخهٔ فشرده در File Manager بگیرید. دیتابیس
را هم از phpMyAdmin با Export یک نسخه پشتیبان بگیرید.

### مرحلهٔ ۲: جایگزینی کد برنامه
۱. فایل `.env` فعلی‌تان را از داخل `navracar-app/` جایی امن کپی کنید — **این
   فایل حاوی اطلاعات واقعی دیتابیس و کلیدهای امنیتی شماست و نباید جایگزین یا
   حذف شود.**
۲. کل محتوای فعلی پوشهٔ `navracar-app/` **به‌جز فایل `.env`** را حذف کنید.
۳. `navracar-app-update.zip` را در همان مسیر (`navracar-app/`) آپلود و
   Extract کنید.
۴. مطمئن شوید فایل `.env` قدیمی‌تان هنوز سر جایش هست.

### مرحلهٔ ۳: جایگزینی فایل‌های عمومی
محتوای پوشهٔ `public_html/` را با محتوای `public_html-update.zip` جایگزین
کنید (`index.php`, `build/`, `.htaccess`, `robots.txt`, `favicon.ico`).

### مرحلهٔ ۴: بررسی نهایی
۱. `https://navracar.com` را باز کنید و صفحهٔ اصلی، محاسبه‌گر (`/calculator`)
   و یک صفحهٔ آگهی خودرو را بررسی کنید.
۲. در محاسبه‌گر و صفحهٔ آگهی، دکمهٔ «دلار» کنار فیلد قیمت را بزنید و مطمئن
   شوید مقدار درست تبدیل می‌شود.
۳. در صفحهٔ یک آگهی خودرو، پایین جدول هزینهٔ واردات را نگاه کنید — باید
   «جدول اقساط وام خرید خارجی خودرو» و «شرایط و مراحل پرداخت» دیده شوند.
۴. دکمهٔ «درخواست پیش‌فاکتور» را بزنید، فرم را پر کنید، PDF را دانلود و باز
   کنید — متن فارسی باید کاملاً خوانا باشد (فونت جدید).

## نکتهٔ فنی دربارهٔ فونت PDF
فونت جدید (Noto Sans Arabic — رایگان و متن‌باز، لایسنس SIL OFL) داخل کد
بسته‌بندی شده و از پوشهٔ `storage/fonts/` به‌عنوان کش موقت دومپی‌دی‌اف استفاده
می‌شود. این پوشه باید توسط PHP قابل نوشتن باشد؛ روی اکثر هاست‌های اشتراکی این
پیش‌فرض برقرار است.

## این بسته قبل از تحویل واقعاً تست شد
یک محیط کامل شبیه‌سازی‌شده (همان آرایش `navracar-app/` + `public_html/` کنار
هم، روی یک دیتابیس MySQL واقعی از قبل migrate‌شده — دقیقاً سناریوی «سایت از
قبل نصب است») ساخته و همهٔ مسیرها بررسی شدند: صفحهٔ اصلی، محاسبه‌گر، صفحهٔ
آگهی، sitemap.xml، بارگذاری فایل‌های CSS/JS. مهم‌تر از آن:
- دکمهٔ دلار در محاسبه‌گر و در جدول محصول عملاً با مرورگر واقعی (Chromium/
  Playwright) کلیک و مقدار تبدیل‌شده تأیید شد.
- جدول اقساط با دادهٔ واقعی یک خودرو تست شد و قانون «سقف ۵۰٪ قیمت برای
  خودروهای ارزان» عددی تأیید شد (مثال دقیق کاربر: خودروی ۱۲ میلیارد تومانی
  → سقف وام ۶ میلیارد تومان).
- **درخواست واقعی پیش‌فاکتور از صفحهٔ آگهی زده شد و فایل PDF واقعی تولید،
  دانلود، و در ۱۵۰dpi رندر شد** — تأیید شد متن فارسی با فونت جدید کاملاً
  خوانا و درست‌شکل است.
